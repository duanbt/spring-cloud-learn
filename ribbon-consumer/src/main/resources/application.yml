server:
  port: 9000

spring:
  application:
    name: ribbon-consumer

eureka:
  client:
    service-url.defaultZone: http://localhost:1111/eureka/
management:
  endpoint:
    hystrix:
      stream:
        enabled: true
  endpoints:
    web:
      exposure:
        include: "*"